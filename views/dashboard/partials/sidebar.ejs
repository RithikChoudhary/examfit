<aside class="dashboard-sidebar">
    <div class="sidebar-header">
        <div class="logo-container">
            <div class="logo">
                <i class="fas fa-graduation-cap"></i>
            </div>
            <div class="brand-info">
                <h2>ExamPrep</h2>
                <p>Admin Dashboard</p>
            </div>
        </div>
    </div>
    
    <nav class="sidebar-nav">
        <div class="nav-section">
            <div class="section-title">Overview</div>
            <a href="/dashboard" class="nav-link <%= currentPage === 'overview' ? 'active' : '' %>">
                <i class="fas fa-th-large"></i>
                <span>Dashboard</span>
            </a>
        </div>

        <div class="nav-section">
            <div class="section-title">Quick Access</div>
            <a href="/dashboard/subjects/upsc" 
               class="nav-link <%= currentPage === 'subjects' && (examId === 'upsc' || (typeof examData !== 'undefined' && examData?.examId === 'upsc')) ? 'active' : '' %>">
                <i class="fas fa-landmark"></i>
                <span>UPSC</span>
            </a>
            
            <a href="/dashboard/subjects/cgl"
               class="nav-link <%= currentPage === 'subjects' && (examId === 'cgl' || (typeof examData !== 'undefined' && examData?.examId === 'cgl')) ? 'active' : '' %>">
                <i class="fas fa-briefcase"></i>
                <span>SSC CGL</span>
            </a>
        </div>

        <div class="nav-section">
            <div class="section-title">Tools</div>
            <a href="/dashboard" class="nav-link">
                <i class="fas fa-chart-bar"></i>
                <span>Analytics</span>
            </a>
            <a href="/dashboard" class="nav-link">
                <i class="fas fa-cog"></i>
                <span>Settings</span>
            </a>
        </div>
    </nav>
</aside>
