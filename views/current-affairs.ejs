<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/navbar.css">
    <link rel="stylesheet" href="/css/home.css">
    <link rel="stylesheet" href="/css/home-footer.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body {
            background-color: #f8fafc;
            margin: 0;
            padding: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        
        .current-affairs-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
            background-color: #f8fafc;
            flex: 1;
        }
        
        /* Override any conflicting footer positioning */
        .modern-footer {
            position: relative !important;
            margin-top: auto;
        }
        
        .current-affairs-header {
            text-align: center;
            margin-bottom: 3rem;
        }
        
        .current-affairs-header h1 {
            font-size: 2.5rem;
            font-weight: 700;
            color: #111827;
            margin-bottom: 1rem;
        }
        
        .current-affairs-header p {
            font-size: 1.125rem;
            color: #4b5563;
            margin-bottom: 2rem;
        }
        
        .filters-section {
            background: white;
            padding: 1.5rem;
            border-radius: 12px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            margin-bottom: 2rem;
        }
        
        .filters-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            align-items: end;
        }
        
        .filter-group {
            display: flex;
            flex-direction: column;
        }
        
        .filter-label {
            font-weight: 600;
            color: #374151;
            margin-bottom: 0.5rem;
        }
        
        .filter-select {
            padding: 0.75rem;
            border: 2px solid #e5e7eb;
            border-radius: 8px;
            font-size: 1rem;
            transition: border-color 0.3s ease;
        }
        
        .filter-select:focus {
            outline: none;
            border-color: #3b82f6;
        }
        
        .filter-btn {
            background: #3b82f6;
            color: white;
            border: none;
            padding: 0.75rem 1.5rem;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.3s ease;
        }
        
        .filter-btn:hover {
            background: #2563eb;
        }
        
        .stats-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: #f9fafb;
            padding: 1rem 1.5rem;
            border-radius: 8px;
            margin-bottom: 2rem;
        }
        
        .stats-text {
            color: #4b5563;
            font-weight: 500;
        }
        
        .last-updated {
            color: #6b7280;
            font-size: 0.875rem;
        }
        
        .affairs-grid {
            display: grid;
            gap: 1.5rem;
        }
        
        .category-section {
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        
        .category-header {
            background: linear-gradient(135deg, #3b82f6, #2563eb);
            color: white;
            padding: 1rem 1.5rem;
            font-weight: 600;
            font-size: 1.125rem;
        }
        
        .affairs-list {
            padding: 0;
        }
        
        .affair-item {
            border-bottom: 1px solid #e5e7eb;
            padding: 1.5rem;
            transition: background 0.3s ease;
        }
        
        .affair-item:last-child {
            border-bottom: none;
        }
        
        .affair-item:hover {
            background: #f9fafb;
        }
        
        .affair-title {
            font-size: 1.125rem;
            font-weight: 600;
            color: #111827;
            margin-bottom: 0.75rem;
            line-height: 1.4;
        }
        
        .affair-content {
            color: #374151;
            line-height: 1.6;
            margin-bottom: 1rem;
        }
        
        .affair-meta {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 1rem;
        }
        
        .affair-badges {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }
        
        .affair-badge {
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 600;
        }
        
        .importance-high {
            background: #fee2e2;
            color: #dc2626;
        }
        
        .importance-medium {
            background: #fef3c7;
            color: #d97706;
        }
        
        .source-badge {
            background: #e5e7eb;
            color: #374151;
        }
        
        .affair-date {
            color: #6b7280;
            font-size: 0.875rem;
        }
        
        .no-affairs {
            text-align: center;
            padding: 4rem 2rem;
            color: #6b7280;
        }
        
        .no-affairs i {
            font-size: 3rem;
            margin-bottom: 1rem;
            color: #9ca3af;
        }
        
        @media (max-width: 768px) {
            .current-affairs-container {
                padding: 1rem;
            }
            
            .current-affairs-header h1 {
                font-size: 2rem;
            }
            
            .filters-grid {
                grid-template-columns: 1fr;
            }
            
            .stats-bar {
                flex-direction: column;
                align-items: flex-start;
                gap: 0.5rem;
            }
            
            .affair-meta {
                flex-direction: column;
                align-items: flex-start;
            }
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>
    
    <div class="current-affairs-container">
        <div class="current-affairs-header">
            <h1><i class="fas fa-newspaper"></i> Current Affairs</h1>
            <p>Stay updated with the latest news and developments important for competitive exams</p>
        </div>
        
        <!-- Filters Section -->
        <div class="filters-section">
            <form method="GET" action="/current-affairs">
                <div class="filters-grid">
                    <div class="filter-group">
                        <label class="filter-label">Category</label>
                        <select name="category" class="filter-select">
                            <option value="all" <%= selectedCategory === 'all' ? 'selected' : '' %>>All Categories</option>
                            <% categories.forEach(category => { %>
                                <option value="<%= category %>" <%= selectedCategory === category ? 'selected' : '' %>>
                                    <%= category %>
                                </option>
                            <% }) %>
                        </select>
                    </div>
                    
                    <div class="filter-group">
                        <label class="filter-label">Importance</label>
                        <select name="importance" class="filter-select">
                            <option value="all" <%= selectedImportance === 'all' ? 'selected' : '' %>>All Levels</option>
                            <option value="high" <%= selectedImportance === 'high' ? 'selected' : '' %>>High Importance</option>
                            <option value="medium" <%= selectedImportance === 'medium' ? 'selected' : '' %>>Medium Importance</option>
                        </select>
                    </div>
                    
                    <div class="filter-group">
                        <button type="submit" class="filter-btn" id="applyFiltersBtn">
                            <i class="fas fa-filter"></i> Apply Filters
                        </button>
                    </div>
                </div>
            </form>
        </div>
        
        <!-- Stats Bar -->
        <div class="stats-bar">
            <div class="stats-text">
                Showing <%= filteredCount %> of <%= totalCount %> current affairs
                <% if (sources.length > 0) { %>
                    | Sources: <%= sources.join(', ') %>
                <% } %>
            </div>
            <% if (lastUpdated) { %>
                <div class="last-updated">
                    Last updated: <%= new Date(lastUpdated).toLocaleDateString('en-IN', { 
                        year: 'numeric', 
                        month: 'short', 
                        day: 'numeric',
                        hour: '2-digit',
                        minute: '2-digit'
                    }) %>
                </div>
            <% } %>
        </div>
        
        <!-- Current Affairs Content -->
        <div class="affairs-grid">
            <% if (Object.keys(groupedAffairs).length === 0) { %>
                <div class="no-affairs">
                    <i class="fas fa-newspaper"></i>
                    <h3>No Current Affairs Found</h3>
                    <p>Try adjusting your filters or check back later for new updates.</p>
                </div>
            <% } else { %>
                <% Object.keys(groupedAffairs).forEach(category => { %>
                    <div class="category-section">
                        <div class="category-header">
                            <i class="fas fa-folder"></i> <%= category %> (<%= groupedAffairs[category].length %>)
                        </div>
                        <div class="affairs-list">
                            <% groupedAffairs[category].forEach(affair => { %>
                                <div class="affair-item">
                                    <h3 class="affair-title"><%= affair.title %></h3>
                                    <p class="affair-content"><%= affair.content %></p>
                                    <div class="affair-meta">
                                        <div class="affair-badges">
                                            <span class="affair-badge importance-<%= affair.importance %>">
                                                <i class="fas fa-star"></i> <%= affair.importance %> importance
                                            </span>
                                            <span class="affair-badge source-badge">
                                                <i class="fas fa-link"></i> <%= affair.source %>
                                            </span>
                                        </div>
                                        <div class="affair-date">
                                            <i class="fas fa-calendar"></i>
                                            <%= new Date(affair.datePublished).toLocaleDateString('en-IN', { 
                                                month: 'short', 
                                                day: 'numeric' 
                                            }) %>
                                        </div>
                                    </div>
                                </div>
                            <% }) %>
                        </div>
                    </div>
                <% }) %>
            <% } %>
        </div>
    </div>
    
    <%- include('partials/footer') %>
    
    <script>
        // Smooth scrolling for any anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
    </script>
</body>
</html>
