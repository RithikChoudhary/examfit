<!DOCTYPE html>
<html lang="en">
<head>
  <!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-5HNLDBJ7EL"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-5HNLDBJ7EL');
</script>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Prepare for competitive exams with ExamFit. Practice previous year papers, explore subject-wise questions, and boost your exam readiness.">
  <meta property="og:title" content="ExamFit – Prepare Smarter" />
  <meta property="og:description" content="Practice competitive exams with real question papers and smart subject-wise prep." />
  <meta property="og:image" content="/templates/logo.webp" />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://examfit.in" />
  <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-7935640093775501"
     crossorigin="anonymous"></script>
  <title>Question Papers - <%= subjectName %> - <%= examName %></title>
  <link rel="stylesheet" href="/css/questionPaper.css"> 
  <link rel="stylesheet" href="/css/navbar.css"> 
  <!-- <link rel="stylesheet" href="/css/dashboard-questionPaper.css"> -->

  <link rel="preload" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" as="style" onload="this.onload=null;this.rel='stylesheet'">
<noscript><link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"></noscript>

  <meta name="google-adsense-account" content="ca-pub-7935640093775501">
  
</head>
<body>
    <%- include('./partials/navbar', { subjects: subjects, exam: exam }) %>

    <main class="subject-page">
        <header class="exam-header">
            <h1><%= subjectName %> Question Papers for <%= examName %></h1>
            <p>Select a Question Paper to start practicing.</p>
        </header>
      
        <div class="content-area">
          <div class="question-paper-sections">
            <!-- <% if (questionPapers.length === 0) { %>
              <div class="no-papers-message">
                <p>No question papers available for this subject yet.</p>
              </div>
            <% } else { %>
              <% const sections = [...new Set(questionPapers.map(paper => paper.section))].sort(); %>
              <% sections.forEach(sectionName => { %>
                <% const papersInSection = questionPapers.filter(paper => paper.section === sectionName); %>
                <% if (papersInSection.length > 0) { %>
                  <div class="section">
                    <h3><%= sectionName %></h3>
                    <ul class="question-paper-list">
                      <% papersInSection.forEach(paper => { %>
                        <li class="question-paper-item">
                          <div class="paper-container">
                            <a href="/<%= exam %>/<%= subject %>/<%= paper.questionPaperId %>/questions">
                              <%= paper.questionPaperName %>
                            </a>
                          </div>
                        </li>
                      <% }); %>
                    </ul>
                  </div>
                <% } %>
              <% }); %>
            <% } %> -->
            <% if (questionPapers.length === 0) { %>
              <div class="no-papers-message">
                <p>No question papers available for this subject yet.</p>
              </div>
            <% } else { %>
              <% const sections = [...new Set(questionPapers.map(paper => paper.section))].sort(); %>
              <% sections.forEach(sectionName => { %>
                <% const papersInSection = questionPapers.filter(p => p.section === sectionName); %>
                <% if (papersInSection.length > 0) { %>
                  <div class="section-block">
                    <h3 class="section-title"><%= sectionName %></h3>
                    <div class="carousel-wrapper">
                      <button class="scroll-btn left" onclick="scrollSectionCarousel('<%= sectionName %>', -1)">&#10094;</button>
                      <div class="carousel" id="carousel-<%= sectionName.replace(/\s+/g, '-') %>">
                        <% papersInSection.forEach(paper => { %>
                          <div class="paper-card-wrapper">
                          <div class="paper-card">
                            <a href="/<%= exam %>/<%= subject %>/<%= paper.questionPaperId %>/questions">
                              <%= paper.questionPaperName %>
                            </a>
                          </div>
                          </div>
                        <% }); %>
                      </div>
                      <button class="scroll-btn right" onclick="scrollSectionCarousel('<%= sectionName %>', 1)">&#10095;</button>
                    </div>
                  </div>
                <% } %>
              <% }); %>
            <% } %>
          </div>
        </div>
    </main>
    <script src="https://kit.fontawesome.com/YOUR-KIT-ID.js" crossorigin="anonymous"></script>

    <script>
      function scrollSectionCarousel(sectionName, direction) {
        const carouselId = 'carousel-' + sectionName.replace(/\s+/g, '-');
        const carousel = document.getElementById(carouselId);
        const card = carousel.querySelector('.paper-card');
        if (!card) return;
    
        const cardStyle = getComputedStyle(card);
        const cardWidth = card.offsetWidth 
                        + parseInt(cardStyle.marginLeft || 0) 
                        + parseInt(cardStyle.marginRight || 0)
                        + 20; // match your .carousel gap in px
    
        carousel.scrollBy({
          left: direction * cardWidth,
          behavior: 'smooth'
        });
      }
    </script>
    

    
    
</body>
</html>

