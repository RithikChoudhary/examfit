<!DOCTYPE html>
<html lang="en">
<head>
   
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Prepare for competitive exams with ExamFit. Practice previous year papers, explore subject-wise questions, and boost your exam readiness.">
  <meta property="og:title" content="ExamFit – Prepare Smarter" />
  <meta property="og:description" content="Practice competitive exams with real question papers and smart subject-wise prep." />
  <meta property="og:image" content="/templates/logo.webp" />
  <meta property="og:type" content="website" />
  <meta property="og:url" content="https://examfit.in" />
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-7935640093775501"
     crossorigin="anonymous"></script>
    <title><%= subject.charAt(0).toUpperCase() + subject.slice(1) %> Questions</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/questions.css">
    <script src="/js/quiz.js"></script>
    <link rel="preload" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" as="style" onload="this.onload=null;this.rel='stylesheet'">
<noscript><link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"></noscript>

    <meta name="google-adsense-account" content="ca-pub-7935640093775501">
    <link rel="stylesheet" href="/css/navbar.css">
  
    <style>
        .question-header h3 {
            white-space: pre-line;
            word-wrap: break-word;
            font-size: 1rem;
            font-weight: normal;
            margin-bottom: 10px;
        }
        .question-block label {
            display: flex;
            align-items: flex-start;
            margin-bottom: 8px;
        }
        .question-block label input {
            margin-right: 10px;
            margin-top: 3px;
        }
        .question-block label span {
            white-space: pre-line;
            word-wrap: break-word;
            flex-grow: 1;
        }
    </style>
    <link rel="stylesheet" href="/css/home-footer.css">
</head>
<body>
    <%- include('partials/navbar', { subjects: subjects, exam: exam }) %>
    <div class="content-wrapper">
    <main>
        <h1><%= subject.charAt(0).toUpperCase() + subject.slice(1) %> Questions</h1>
        <% if (questions.length === 0) { %>
            <p>No questions available for this subject.</p>
        <% } else { %>
            <% questions.forEach((q, index) => { %>
                <div class="question-block">
                    <div class="question-header">
                        <div class="question-number">#<%= index + 1 %></div>
                        <h3><strong><%= q.question %></strong></h3>
                    </div>
                    <% q.options.forEach(option => { %>
                        <label>
                            <input 
                                type="radio" 
                                name="question-<%= q.questionId %>" 
                                value="<%= option.optionId %>"
                                onclick="checkAnswer(event, '<%= q.correctOption %>', `<%= q.explanation %>`)"
                            >
                            <span><%= option.text %></span>
                        </label>
                    <% }) %>
                    <div class="explanation" style="display: none;"></div>
                </div>
            <% }) %>
            <button onclick="calculateResults()" class="submit-button">
                Submit Quiz
            </button>
        <% } %>
    </main>
</div>
    <%- include('./partials/footer') %>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
      
        if ('requestIdleCallback' in window) {
          requestIdleCallback(() => {
            const gtagScript = document.createElement('script');
            gtagScript.src = "https://www.googletagmanager.com/gtag/js?id=G-5HNLDBJ7EL";
            gtagScript.async = true;
            document.head.appendChild(gtagScript);
      
            gtag('js', new Date());
            gtag('config', 'G-5HNLDBJ7EL');
          });
        } else {
          // Fallback if browser doesn't support requestIdleCallback
          setTimeout(() => {
            const gtagScript = document.createElement('script');
            gtagScript.src = "https://www.googletagmanager.com/gtag/js?id=G-5HNLDBJ7EL";
            gtagScript.async = true;
            document.head.appendChild(gtagScript);
      
            gtag('js', new Date());
            gtag('config', 'G-5HNLDBJ7EL');
          }, 2000);
        }
      </script>
</body>
</html>